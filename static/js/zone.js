// Placeholder for captive portal zone configuration.
// The production environment is expected to provide the correct zone identifier.
if (typeof window !== 'undefined' && typeof window.zoneid === 'undefined') {
  window.zoneid = '';
}
